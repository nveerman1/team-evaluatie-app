# Frontend Environment Configuration - Production
# Use this for production deployment

# Disable dev-login in production (SECURITY CRITICAL)
NEXT_PUBLIC_ENABLE_DEV_LOGIN=false

# Backend API Base URL (optional - defaults to /api/v1)
# For same-origin deployment (nginx proxy), use relative path or omit (uses default):
# NEXT_PUBLIC_API_BASE_URL=/api/v1
# For external API server, use full URL:
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com/api/v1
