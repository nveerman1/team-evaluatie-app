name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  backend:
    name: Backend (FastAPI) – Lint/Type/Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Ensure CI requirements file exists
        run: |
          if [ ! -f requirements-ci.txt ]; then
            cat > requirements-ci.txt << 'EOF'
fastapi
uvicorn[standard]
pydantic-settings
python-multipart
sqlalchemy
psycopg2-binary
alembic
passlib[argon2]
pyjwt
python-jose[cryptography]
redis
rq
boto3
pydantic[email]
email-validator
ruff
black
mypy
pytest
EOF
          fi

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/requirements-ci.txt

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-ci.txt

      - name: Ruff (lint)
        if: ${{ ha
